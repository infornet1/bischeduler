-- Diagnose monthly_attendance_summary table structure
-- Check which fields are NOT NULL

USE ueipab_2025_data;

-- Show full table structure with NULL/NOT NULL info
SELECT 
    COLUMN_NAME,
    COLUMN_TYPE,
    IS_NULLABLE,
    COLUMN_DEFAULT,
    COLUMN_KEY
FROM 
    INFORMATION_SCHEMA.COLUMNS
WHERE 
    TABLE_SCHEMA = 'ueipab_2025_data'
    AND TABLE_NAME = 'monthly_attendance_summary'
ORDER BY 
    ORDINAL_POSITION;
